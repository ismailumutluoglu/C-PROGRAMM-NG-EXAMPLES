// strcopy fonksiyonu
#include <stdio.h>
#include <string.h>
char *copy_str(char* str1,char* str2);
int main()
{
    char str1[50];
    char str2[50];
    copy_str(str1,str2);
    return 0 ; 
}
char *copy_str(char* str1,char* str2)
{
    char *ptr  = str1;
    while(*str1 != '\0')
    {
        
    }
}